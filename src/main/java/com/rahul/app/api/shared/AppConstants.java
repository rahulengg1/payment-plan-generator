package com.rahul.app.api.shared;

import java.math.BigDecimal;

public interface AppConstants {

	static final BigDecimal DAYS_PER_MONTH = BigDecimal.valueOf(30);
	static final BigDecimal DAYS_PER_YEAR = BigDecimal.valueOf(360);
	static final int DECIMALS = 2;
	static final int PAYMENTS_PER_YEAR = 12;
}
